#include<stdio.h>

// This program uses only stdio.h header calls ( printf and C loop functions such as if, while, switch)

void main(){
    if(printf("Hello world")){ // This can be substitued with while(!printf("Hello world")) or switch(printf("Hello world"))
    }
}
